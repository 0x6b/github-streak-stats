[package]
name = "github-streak-stats-lib"
version = "0.3.0"
edition = "2021"
description = "A library for fetching GitHub streak stats."

[dependencies]
chrono = "0.4.26"
graphql_client = { version = "0.13.0", features = ["reqwest-blocking", "reqwest-rustls"] }
reqwest = { version = "0.11.18", features = ["blocking"] }
serde = "1.0.164"
serde_json = "1.0.96"
